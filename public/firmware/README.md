Coloque aqui os arquivos .bin publicados para OTA.

Exemplo:
- public/firmware/esp32-gateway-1.0.0.bin

O equipamento deve consultar /ota/manifest.json, comparar a versao atual com latestVersion e baixar o firmware.url correspondente.
